repos:
  - repo: local
    hooks:
      - id: pi-snapshot
        name: Validate portable Pi snapshot
        entry: node pi/check.mjs
        language: system
        pass_filenames: false
        files: ^pi/
