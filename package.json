{
  "name": "agent-skills",
  "version": "0.1.0",
  "private": true,
  "description": "Agent Skills for Guild Wars automation and Python development.",
  "keywords": [
    "agent-skills",
    "pi-package"
  ],
  "license": "MIT",
  "scripts": {
    "test": "node --test pi/*.test.mjs && npm --prefix pi/extensions test",
    "check": "node pi/check.mjs && npm --prefix pi/extensions run check",
    "lint": "node pi/extensions/node_modules/oxlint/bin/oxlint --import-plugin --deny-warnings pi"
  },
  "pi": {
    "skills": ["./skills"]
  }
}
